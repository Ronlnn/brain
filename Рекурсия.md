---
tags:
  - knowledge
zero-links: "[[00 Algorithms]]"
created: 09-11-25
prompt:
---
# 📑 Рекурсия 
- Это когда функция вызывает сама себя 

### Базовый случай и рекурсивный случай
- Базовый случай - это когда функция сама себя не вызывает
- Рекурсивный случай - когда вызывает 
---
При работе с рекурсией важно в функции сделать оба случая.
Для чего это?
Чтобы не было бесконечного цикла. То есть важно понимать когда прервать рекурсию.

---
Пример 
```js
function countDown(i) = {
	console.log(i);
	if(i <= 0){
		return; //Базовый случай
	}else {
		contDown(i - 1);  // Рекурсивный случай 
	}
}
```
- Если мы не добавим базовый случай, то рекурсия будет бесконечно и уйдет в -. -1, -2 и тд

